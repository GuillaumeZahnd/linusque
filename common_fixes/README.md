# Common fixes

## Network connection down after kernel update

1. Reboot in advanced mode by pressing the `Shift` key.

2. Choose an older kernel version to boot on.

3. Run the update process again:

```sh
sudo apt upgrade
sudo apt update
```

4. Reboot.
